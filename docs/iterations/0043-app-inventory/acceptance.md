# ITER-0043 Acceptance

> 状态：Active
> 更新日期：2026-07-21

- [x] 应用清单单次最多返回 500 个应用，并支持安全前缀过滤。
- [x] 单应用详情只返回应用标识、版本、安装来源和启用状态。
- [x] Android Adapter 只构造固定 ADB 参数数组，并在进程调用前拒绝非法应用标识。
- [x] Capability 缺失、设备离线和应用不存在返回统一领域错误。
- [x] REST 与 MCP 入口均不直接访问 ADB，并标记为只读、幂等。
- [x] APK 路径、签名、权限和原始平台输出不进入公共响应。
- [x] 默认测试、类型检查、格式检查和 Contract 形状回归全部通过。
- [x] 真机通过 MCP 列出 344 个匹配应用并有界返回前 20 个，未产生设备变更。
- [x] 真机通过 MCP 查询 `com.android.settings`，返回版本 `14.0.0.210` 和版本代码 `14000210`；缺失字段保持 `null`，不推断或伪造状态。
